#include<iostream>
#include<stdio.h>
using namespace std;
int main(){
    freopen("catchcow.in","r",stdin);
    freopen("catccow.out","w",stdout);
    int n,k,time=0;
    scanf("%d,%d",&n,&k);
    if(k==n)
        {time=0;
    printf("%d",&time);}
    else
        printf("%d",&time);
    fclose(stdin);
	fclose(stdout);
}
